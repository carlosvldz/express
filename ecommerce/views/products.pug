ul
    each product in products
        li
            // variable dentro de tag=
            strong= product.name
            p= product.price
            // texto plano p (sin igual)
            p Comprar